// Permanent App Store identity shared by the iOS and macOS apps.
PRODUCT_BUNDLE_IDENTIFIER = de.groon.konta
DEVELOPMENT_TEAM = 5XAJ947826
CODE_SIGN_STYLE = Automatic
MARKETING_VERSION = 1.0.1
CURRENT_PROJECT_VERSION = 2
SWIFT_VERSION = 6.0
// Escape the URL separator because // starts an xcconfig comment.
KONTA_API_URL = https:/$()/konta-finance.com
#include? "Signing.local.xcconfig"
